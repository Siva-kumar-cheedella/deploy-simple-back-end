# ─── Server ───────────────────────────────────────────────
server.port=8098

# ─── Database (Supabase PostgreSQL - Session Pooler) ──────
# Replace these with your actual Supabase values
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.htglcomsyyjncwasqbiu
spring.datasource.password=8106236702@Si#
spring.datasource.driver-class-name=org.postgresql.Driver


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ─── JWT ──────────────────────────────────────────────────
# Generate a strong secret: openssl rand -hex 64
app.jwt.secret=REPLACE_WITH_A_LONG_RANDOM_SECRET_KEY_AT_LEAST_64_CHARS_LONG_FOR_HS256
app.jwt.expiration-ms=86400000

# ─── CORS ─────────────────────────────────────────────────
# Add your Netlify URL here
app.cors.allowed-origins=http://localhost:3000,https://your-site.netlify.app

logging.level.root=INFO
logging.level.com.goodmorning=DEBUG
